## Packages
framer-motion | Page transitions and scroll-triggered animations
@hookform/resolvers | Form validation with Zod
react-hook-form | Form state management
lucide-react | Icons

## Notes
Static images: import optimusImage from '@assets/optimus_1773103843324.jpg'.
API uses POST /api/register with z.object({ name, email, phone }).
Dark mode styling is enforced by default in index.css to match the sleek Tesla aesthetic.
